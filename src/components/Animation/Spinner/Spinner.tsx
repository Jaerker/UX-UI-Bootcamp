import './spinner.css';

const Spinner = () => {
  return (
    <section className='spinner_wrapper'>
      <section className="spinner_rotation-part">
        <figure className='spinner'></figure>

      </section>
    </section>
  )
}

export default Spinner